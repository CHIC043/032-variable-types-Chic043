// Define a string variable named animalName
let animalName = "Cat";

// Define a numeric variable named height
let height = 25;

// Define a boolean variable named isIndoorPet
let isIndoorPet = true;

// Assign the innerHTML of the HTML element with the id result to the animalName
document.getElementById('result').innerHTML = animalName;
